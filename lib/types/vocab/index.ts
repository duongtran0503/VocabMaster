export interface VocabType {
    word: string;        // Từ tiếng Anh
    phonetic: string;    // Phiên âm
    meaning: string;     // Nghĩa tiếng Việt
    example: string;     // Câu ví dụ
    category: string;    // Danh mục (e.g., "Food", "Job")
    image: string;       // Link ảnh minh họa
};